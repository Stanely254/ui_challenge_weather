name: ui_challenge_weather
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  json_annotation: ^4.8.1
  freezed_annotation: ^2.4.1
  app_utils:
    git:
      url: https://github.com/humble-house/app_utils.git
  google_fonts: ^6.1.0
  material_color_utilities: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  freezed: ^2.4.5
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  assets: 
    - assets/icons/

  
